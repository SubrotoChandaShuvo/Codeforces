#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a;
    getline(cin, a);
    int b=a.size();
    for(int i=0; i<b; i++)
    {
        if(a[i]=='\\')
        break;
        cout<<a[i];
    }

}
